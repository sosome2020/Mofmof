<%= form_with(model: station, local: true) do |form| %>
  <% if station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(station.errors.count, "error") %> prohibited this station from being saved:</h2>

      <ul>
      <% station.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :railway_name %>
    <%= form.text_field :railway_name %>
  </div>

  <div class="field">
    <%= form.label :station_name %>
    <%= form.text_area :station_name %>
  </div>

  <div class="field">
    <%= form.label :walking_minutes %>
    <%= form.number_field :walking_minutes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
